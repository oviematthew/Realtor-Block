module.exports = {
  plugins: {
    "@tailwindcss/postcss": {},
    autoprefixer: {},
  },
};

// This PostCSS configuration file sets up Tailwind CSS and Autoprefixer as plugins.
// It is used to process CSS files in a Next.js application, ensuring that Tailwind's utility classes and vendor prefixes are applied correctly.
